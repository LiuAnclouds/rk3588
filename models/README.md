# Models

Place RKNN models in this directory on the Orange Pi board.

Current runtime default:

```text
models/current_seg.rknn -> models/yolov8n_seg_rk3588_i8.rknn
```

Model files are not tracked in Git because they are binary artifacts. Keep them
on the deployment board or attach them as release assets if needed.

